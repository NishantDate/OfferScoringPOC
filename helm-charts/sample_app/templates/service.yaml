apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample_app.fullname" . }}
  labels:
    {{- include "sample_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample_app.selectorLabels" . | nindent 4 }}
